name: "v2.0.x: release"

on:
  push:
    tags:
      - 2.0.**

jobs:
  release:
    name: Release
    permissions:
      contents: write
      packages: write
      discussions: write
    uses: ./.github/workflows/template-release.yml
    secrets: inherit
    with:
      platforms: linux/amd64,linux/arm64
      directory: v2.0.x
