name: "v1.9.x: build"

on:
  push:
    branches:
      - main
    paths:
      - v1.9.x/**

  pull_request:
    branches:
      - main
    paths:
      - v1.9.x/**

jobs:
  test:
    name: Test
    uses: ./.github/workflows/template-build.yml
    with:
      platforms: linux/amd64,linux/arm64
      directory: v1.9.x
