name: "v2.0.x: build"

on:
  push:
    branches:
      - master
    paths:
      - v2.0.x/**

  pull_request:
    branches:
      - master
    paths:
      - v2.0.x/**

jobs:
  test:
    name: Test
    uses: ./.github/workflows/template-build.yml
    with:
      platforms: linux/amd64,linux/arm64
      directory: v2.0.x
